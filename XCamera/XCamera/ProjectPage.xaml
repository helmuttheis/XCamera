<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XCamera.ProjectPage">
    <ContentPage.Content>
        <AbsoluteLayout>
            <Grid AbsoluteLayout.LayoutBounds="0, 0, 1, 1"
                 AbsoluteLayout.LayoutFlags="All">
                <StackLayout>
                <Label Text="Projektliste"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
                <ListView x:Name="lstProjects" ItemTapped="LstProjects_ItemTapped" >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout class="projectViewCell" Orientation="Horizontal" HorizontalOptions="Fill" BackgroundColor="Olive">
                                    <Label Text = "{Binding .}" FontSize="24" HorizontalOptions="End" AbsoluteLayout.LayoutBounds="0.25, 0.25, 400, 40"/>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Button x:Name="btnConnect" Clicked="BtnConnect_Clicked" Text="verbinden"/>
                    <Button x:Name="btnNew" Clicked="BtnNew_Clicked" Text="neu"/>
                </StackLayout>
            </Grid>
            <!-- Overlay -->
            <ContentView x:Name="viewOverlay"
                 AbsoluteLayout.LayoutBounds="1, 1, 1, 1"
                 AbsoluteLayout.LayoutFlags="All"
                 IsVisible="False"
                 BackgroundColor="#C0808080"
                 Padding="10, 10,10,10"
                 Margin="10, 10,10,10"
                         >
                
                <Grid x:Name="grdOverlay"
                   HorizontalOptions="Center" 
                   VerticalOptions="Center" >
                </Grid>


            </ContentView>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>